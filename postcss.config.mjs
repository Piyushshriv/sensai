import tailwindcss from 'tailwindcss';
import autoprefixer from 'autoprefixer';

export default {
  plugins: [
    tailwindcss(),   // Ensure you're using the correct version of tailwindcss here
    autoprefixer(),
  ],
};
